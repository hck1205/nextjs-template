export type SignUpInfo = {
  email: string;
  nickname: string;
  password: string;
  passwordConfirm: string;
};

export type SignUpInfoBool = {
  email: boolean;
  nickname: boolean;
  password: boolean;
  passwordConfirm: boolean;
};
